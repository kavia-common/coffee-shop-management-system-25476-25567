All image assets under this folder are served statically by the app at runtime.
Place Figma-exported images here and reference them in code using absolute paths like:
/assets/figmaimages/your_image.png

This prevents relative path issues across nested routes (e.g., /screens/cafe).
